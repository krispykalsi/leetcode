//// Created by I$K on 29/09/21.//#include "list-node.cpp"class Solution {public:    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {        auto l = new ListNode(-1);        auto dummy = l;        int carry = 0;        while (l1 != nullptr || l2 != nullptr) {            int sum = carry;            if (l1 != nullptr) {                sum += l1->val;                l1 = l1->next;            }            if (l2 != nullptr) {                sum += l2->val;                l2 = l2->next;            }            if (sum > 9) {                l->next = new ListNode(sum % 10);                carry = 1;            } else {                l->next = new ListNode(sum);                carry = 0;            }            l = l->next;        }        if (carry == 1) {            l->next = new ListNode(1);        }        auto temp = dummy->next;        delete dummy;        return temp;    }};